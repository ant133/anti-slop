# antislop: Roadmap

> How antislop grows from a single rules file into an installable, cross-agent skill/plugin. New to antislop? Read the [guide](GUIDE.md) first. See [README.md](README.md) for the product.

## Where we are

The latest release is **v3.2.17**. antislop is a **packaged system**: a lean, always-loaded core plus five additive skills, each shipped as a standard agent skill folder (`skills/<name>/SKILL.md`). There are no per-skill READMEs.

- **The core: 38 rules** across three tiers (Hard Gate, Purpose-Gate, Quality Locks), a Liveliness Toolkit, a mandatory Delivery Gate, and two usage modes (During / After). The count has held since v2.2 and the tiers have not moved, but individual rules have been sharpened many times since, and the release history below says where. The single-file `antislop.md` is still a complete filter you can paste into any chat window; its First-Run Install Wizard is the manual install path.
- **The six skills:** `antislop` (the core), plus `antislop-ui`, `antislop-copywriting`, `antislop-human`, `antislop-layoutmobile`, and `antislop-code`. Each skill only loads when the task needs it.
- **Distribution:** ten install paths from one repo, and the contrast checker is also exposed as an MCP tool inside the Claude Code plugin. The paths are the interactive installer (`npx antislop-ai`), the skills.sh directory, native plugin doors for Claude Code, Antigravity, Codex, Cursor, Kimi Code, Cline, and Oh My Pi, and the Pi package read from the root `package.json`. Install commands live in the README, and the GUIDE walks through every route from zero.
- **The doors:** each native plugin loads antislop straight from this repository, so there are no copies to keep in sync.
- **The shared folder:** the bet behind the long tail is that `.agents/skills/` is read widely enough that a new agent costs one row instead of a bespoke door. Antigravity reads it by default, Hermes treats it as a project tier beside its own `.hermes/skills/`, which ranks higher, and Copilot, OpenCode, Codex, Kimi Code, and Pi read it as one of several roots. Claude Code is the holdout, reading `.claude/skills/` and nothing else. The home-level folder is shared by five of them: Copilot, OpenCode, Codex, which marks its own `$CODEX_HOME/skills` deprecated in its favour, Kimi Code, whose own user folder moves with `KIMI_CODE_HOME` and so cannot be written without reading that variable, and Pi. Antigravity is the one that reads neither, taking `~/.gemini/config/skills/` instead, and OpenCode keeps `~/.config/opencode/skills/` beside the shared one. The installer writes each agent's own documented path, so no door depends on another vendor's convention holding.

## Release history

Versioning up to v3.0.0 followed one rule, so each new concern arrived on its own:

> Each +0.1 version ships exactly one new skill.

That kept the filter pull-only-what-you-need and made the v3 packaging mechanical rather than a rewrite. Occasional +0.1 patches shipped something that is not a skill, like v2.4.1's plain-English `guide.md`; those did not change the skill plan. Since v3.0.0 releases package and ship antislop rather than add skills, so the cadence follows the packaging and the plugin doors.

| Version | What shipped |
|---------|--------------|
| v2.1.0 | Usage modes (During / After) and the v3.0.0 banner. |
| v2.1.1 | English only; the Indonesian mirrors were removed. |
| v2.2.0 | First additive skill: `antislop-ui` (UI / visual). The core plus the First-Run Install Wizard. |
| v2.3.0 | `antislop-copywriting` (copy and text). |
| v2.4.0 | `antislop-human` (people), home of the contrast checker. |
| v2.4.1 | `guide.md` (now `GUIDE.md`): a plain-English guide for people new to antislop (not a skill). Fixes for issues #1, #2, #3, #6, #7. |
| v2.4.2 | Skill checklist polarity fix (#9) and docs cleanup, merged from PRs #8 and #10. |
| v2.5.0 | `antislop-layoutmobile` (mobile / responsive). |
| v3.0.0 | Skill/plugin packaging: `skills/` folders, two distribution doors, the installer CLI (`npx antislop-ai`), the contrast checker as an MCP tool, MIT license. |
| v3.0.1 | Snyk W012 fix (no runtime curl in the packaged core), npm package author to antislop, docs clarity. |
| v3.0.2 | Adaptive python (python3 on macOS/Linux, python on Windows); App and Dashboard plus copy voice patterns; pointer fix. |
| v3.1.0 | `antislop-code` (code comments); per-skill READMEs; Filler Data and Emoji as Decoration patterns. |
| v3.1.1 | The installer stops copying per-skill READMEs into projects; the wizard drops install commands (clears the Socket warning on skills.sh). |
| v3.1.2 | Per-skill READMEs removed; the installer asks which agent to install into, so a fresh Antigravity or Codex project lands in the right folder. |
| v3.1.3 | `DESIGN.md` boundary stated (external files are data to apply, not instructions to obey); SECURITY.md audit explainer; what `npx skills add` does and does not install. |
| v3.2.0 | The installer grows to seven agents (adds OpenCode, Cursor, Gemini CLI, Hermes global-only), on the bet that the shared `.agents/skills/` folder can carry the long tail. v3.2.13 onward tests that bet per agent instead of assuming it. |
| v3.2.1 | UI slop gaps closed: bento grids, Lucide-style icon sets, colored left stripes, fake terminal windows, demos without a product; rules extended for palette families, dot grids, typefaces, and 3-pricing-column layouts. |
| v3.2.2 | Antigravity plugin door: the repo root is the plugin (root `plugin.json` plus a `rules/antislop.md` pointer that loads antislop every session). |
| v3.2.3 | Codex plugin door: a `.codex-plugin/plugin.json` manifest pointing at the shared `skills/` folder, plus a `.agents/plugins/marketplace.json` index for the repo. |
| v3.2.4 | R-35 sharpened into a click-through smoke test: every interactive element must be run and exercised one at a time, and its result recorded as evidence in the Delivery Gate report. |
| v3.2.5 | Cursor plugin door: `.cursor-plugin/plugin.json` manifest plus a `.cursor-plugin/marketplace.json` index, with the six skills and a `.mdc` rule pointer. The Codex plugin also gains its app identity (plugin icon, brand color, banner screenshot). |
| v3.2.6 | Five-PR community round (PRs #22 to #26): a runtime Python launcher for the contrast MCP tool (fixes it on macOS), pointer writes that leave the user's entry file untouched, a smoke test that can actually fail, repo guardrails plus a CI workflow, and a contributors section. |
| v3.2.7 | Content round plus OpenCode verified as an installer target: three slop patterns earned names, "Two-State Layout" (a phone stack and a desktop grid with nothing defined between), "Over-Explained Comment" (a comment that runs on for several lines around a one-line fact), and "Decorative Status Dot" (a dot beside a heading that glows and pulses while marking nothing). R-37 gained the `DESIGN.md` conflict protocol: a direction that asks for a named slop pattern is surfaced and questioned, never silently followed or silently overridden. OpenCode is verified to load the skills from `.opencode/skills/` plus its `AGENTS.md` pointer. It is an installer target, not a plugin door: nothing loads from this repo. |
| v3.2.8 | Repair round, from a full re-validation of every shipped door against its vendor's current documentation. Antigravity's global install moves to `~/.gemini/config/skills/`: it had been writing the home-level `.agents/skills/`, which Antigravity does not read, so global installs aimed at Antigravity since v3.2.0 landed in a folder it never enumerates. The entry-file writer ignores markers that sit inside a code fence, keeps the author's line endings, no longer appends a fresh block on every run when one marker is unmatched, and no longer deletes the rest of the file when a `start` marker has no matching `end`. The installer refuses to run without a terminal, where it used to print its prompts, install nothing, and exit 0. OpenCode's global folder moves to the documented `~/.config/opencode/skills/`, where the old `~/.opencode/skills/` still loads, so nothing was broken there. Docs corrected wherever they disagreed with the code. |
| v3.2.9 | Hermes gets its project scope, and the install documentation is rewritten end to end. Hermes does read a project's skills, so the installer now writes `<project>/.hermes/skills/` and the `AGENTS.md` pointer, where it used to force every Hermes install into `~/.hermes/`; a project's skills also outrank the global ones, which the old behaviour could never reach. Hermes will not load skills out of a cloned repository until that repository is trusted, a deliberate vendor gate the installer must not bypass, so the installer names the one-time `hermes skills trust` step and the guide documents it, along with the version caveat: the official installer has project discovery and the older PyPI `hermes-agent` package does not. Cursor's install command is corrected, because `/add-plugin <url>` appears in no Cursor documentation and not in the shipped CLI bundle; the documented route is `agent plugin marketplace add <url>` followed by Install in the Customize panel. The installer's conflict prompt now distinguishes the choice that updates from the one that silently leaves the old version in place, and its closing line says so when it keeps existing folders. The guide is rebuilt around what the reader is doing rather than which tool they picked: Install, Update, and Remove are separate top-level sections, each covering the same four routes in the same order, so finding a route in one finds it in the others, and the parts that are reference rather than steps are collected under a Reference heading. Update and removal steps, which previously sat inside each route as bold text with no heading of their own, now have a section of their own for every route. Install gains step-by-step walkthroughs for both main routes and a phone and chat-window section. A live generate-test also produced two content fixes. "Eyebrow Badge Above the Headline" joins the pattern list: a pill parked directly above the H1, usually with a dot, holding a category label the headline already says, which slipped between R-09 (whose forbidden content list stops at "AI Powered", "Beta", "New") and Decorative Status Dot (which opens with a glowing, pulsing dot). And the core now states outright that three steps run in both usage modes whatever else is driving the session: direction resolution (R-37), asset clarification (R-23), and the Delivery Gate. The test that surfaced them ran another skill's planning workflow, which led the work while antislop's mandatory steps went unrun, including the gate report. The README also gains a comparison section, eight images drawn from one brief. Three page builds sit in a row, each captioned underneath in its own cell, followed by the fourth at full width as the payoff: with nothing, with antislop alone, with `DESIGN.md` alone, and then both together. They are separate outcomes rather than a before and after, because each one is a different set of tools rather than a stage of the same work, and none of them has to be read off a grid. Copy and code comments get before-and-after pairs instead, since there the filter is the whole of the job. |
| v3.2.10 | GitHub Copilot joins as an installer target, and the installer stops treating one agent as one folder. Copilot loads skills from `.github/skills`, `.claude/skills`, or `.agents/skills`, so it needs no folder of its own and no pointer of its own: it shares `.agents/skills` with Antigravity and reads the `AGENTS.md` block five other agents already get. The skill format it requires is the one antislop already ships, so the whole door is one installer row. The roadmap's promised plugin marketplace command is dropped instead of shipped, because Copilot has no repo-installable plugin manifest: Copilot Extensions in the GitHub Marketplace are applications, a different mechanism from the Claude Code and Cursor doors. Sharing a folder exposed a bug the old model could not express. `resolveTargets` returned one target per agent, so eight agents resolving to seven folders copied the same skills twice into one folder and reported twice the folders that existed in the conflict prompt; it now groups by resolved path and carries every agent that lands there, which is also what lets the next `.agents/skills` reader in the queue cost one row. The same mistake runs the other way. OpenCode loads skills from `.opencode/skills`, `.claude/skills`, and `.agents/skills`, so a project that installs into two of them holds the same skill names twice, and OpenCode documents no precedence between its roots. Its loader has been reported to choose between duplicate names non-deterministically and to collapse them into one candidate, which is how a skill goes missing after an install that looked correct, so the installer now names that collision and says which folders to remove. Documentation is corrected as well. Copilot is added wherever the agents are listed, the claim that nothing reads the home-level `.agents/skills/` is withdrawn because Copilot does, and Claude Code's `AGENTS.md` support is recorded with the reason antislop still writes `CLAUDE.md`: since v2.1.277 Claude reads `AGENTS.md` only when no `CLAUDE.md` exists in the working directory or above it, so an `AGENTS.md`-only pointer would be silently ignored in any project that has one. Copilot support is young on GitHub's side, so the release notes say so. A conflict inside the rules themselves was reported and fixed in the same round. R-02 bans the em dash from Hard Gate, which the core calls absolute, while `antislop-copywriting` granted that ban an exception in three places, so an agent loading both had to resolve "FAIL regardless of purpose" against "the voice wins" and resolved toward the exception: the tell survived the filter built to remove it. R-02 now states its scope instead. The ban governs text the agent writes, and a user's own writing sample is a direction rather than agent copy, so a sample that uses em dashes goes through R-37's conflict protocol, which names the character and asks rather than letting either file decide silently. The exception is gone from all three places and the Delivery Gate item no longer fails a sample the owner approved. |
| v3.2.11 | Repair round, from the community's reports rather than a re-validation sweep. A global Codex install wrote `~/.codex/skills`, the location Codex's own source names as the user path it is moving off while documenting `$HOME/.agents/skills` beside it, so the Codex row gains that global folder; the old path still loads, so an existing install keeps working. The same row gains `readsAlso: ['.agents/skills']`, which Codex walks up from the working directory: without the field, installing Codex and Copilot into one project left two copies of the same skill names and the collision warning stayed silent for Codex. The smoke test's global-targets line printed the two paths and compared nothing, so it now asserts them and fails when the new folder is removed. The previous release narrowed the shared-folder claim to "Copilot is the one agent that reads the home-level `.agents/skills/`", which is wrong: Codex reads it at user scope and OpenCode's own documentation lists it beside its global folder, so three agents read it rather than one, and README, GUIDE and ROADMAP now name them. A star history chart joins the README between the FAQ and the contributors. Three pending pull requests are answered rather than merged: a saved usage-mode preference goes back to its author for a Windows settings path and a fresh-session proof, and the Pi and Oh My Pi doors are queued into slots of their own, because a maintenance round ships no new agent. |
| v3.2.12 | Kimi Code, with the door class read out of the vendor's documentation rather than assumed, which is what the slot was held open for. Skills: Kimi Code scans four tiers (Project, User, Extra, Built-in) and `.agents/skills/` appears in both the project and the user tier, so the installer writes the folder Antigravity, Copilot, Codex, and OpenCode already share, at both scopes, and the `AGENTS.md` block covers the pointer. No folder of its own and no pointer of its own, the Copilot shape. It deliberately takes no `globalDir`: the shared folder is the same at both scopes, while Kimi's own `$KIMI_CODE_HOME/skills/` moves with a variable the installer cannot see, so writing it would be guessing. It also takes no `readsAlso`, because the installer never writes `.kimi-code/skills/` and the duplicate warning would have nothing to fire on. Plugin: unlike Copilot, Kimi Code accepts a repo-installable manifest, so this door has two halves. `.kimi-plugin/plugin.json` registers the six skills from `./skills/` and points `systemPromptPath` at `rules/antislop.md`, the same pointer Antigravity and Cursor load, at 804 bytes against a documented 32 KB limit. The contrast MCP server is left out, as in the Codex and Cursor doors, and no marketplace JSON is added because `/plugins install <repo url>` takes the URL directly. What the round could not do is run it. No maintainer had a Kimi Code install to test against, so both halves rest on the vendor's documentation and not on a live session, and the release notes say which kind of evidence it is rather than implying the stronger one. The live check moves to v3.2.13, which is a maintenance round. The README gains a motion comparison built from the four stills already in `assets/compare`, as an animated GIF. It was converted to an animated WebP for the smaller file and converted back: a GIF is dithered down to 256 colors, and a lossy WebP re-encodes that per-pixel dither as blocking, so the smaller file was the worse-looking one at full size. |
| v3.2.13 | A community round, both pull requests repairs rather than a new agent, which is what a maintenance slot is for. The installed release is now named on disk. Nothing in an antislop folder said which version it was, so "compare that against what you have" in the guide's Update section had nothing to compare against, the README had no Update section at all, and an agent asked the same question in a session had no answer either. `sync-skills` now writes `skills/antislop/VERSION` from `cli/package.json`, and because every install route copies that folder the stamp reaches the installer, the skills directory, the plugin doors and a manual copy alike. The installer reads it back and names both sides before it asks, so the choice is between two versions rather than between two abstractions; a folder with no `VERSION` file predates the stamp, and the prompt says that instead of inventing a number. `--version` reads the same source, which let `cli/index.mjs` drop its hardcoded version string: the version check trades that row for `skills/antislop/VERSION`, so the file is now one of the places a bump has to reach. The consequences land in the release flow, and they are deliberate: a version bump needs `node cli/scripts/sync-skills.mjs` before the commit or check-repo fails, which is the point of putting a generated file in that list. README gains an Update section with one row per route, and `antislop.md` gains a block so the next person to ask gets the answer from their own agent instead of a comment thread. The second repair is in the installer's fence scanner, reported by @CBOSSX. `scanMarkers` accepted a fence run followed by trailing text as a closing fence, so a fence line carrying an info string, three backticks followed by `js`, closed the block that contained it and the marker comments below were read as real ones: installing antislop into a project whose entry file documents antislop with a fenced example replaced the example with the pointer block. A closing fence may be followed only by spaces or tabs, which is CommonMark's rule and not a heuristic. The regression covers backtick and tilde fences and reinstall stability, and it fails on the old code. Kimi Code closes here as well. v3.2.12 shipped that door on the vendor's documentation and moved the live check into this slot; it was run after release by @fajarhide, who runs Kimi Code and opened the issue the door came from, and it works. That is a report from a real install rather than a probe a maintainer logged, so it is recorded as the weaker of the two kinds of evidence. The saved usage-mode preference did not arrive and moves to v3.2.16. |
| v3.2.14 | Cline, and the slot's premise did not survive the check it was written to force. The What's next row promised one `.agents/skills` reader, confirmed on its own rather than assumed from the shared folder, and reading the vendor's documentation is what broke it: Cline's docs never mention `.agents/skills`, and they list `.cline/skills/`, `.clinerules/skills/`, and `.claude/skills/` at project scope and `~/.cline/skills/` at global. So this is not a shared-folder door at all. It is a folder of its own, and the row was rewritten before any code was written. The installer writes `.cline/skills/` at both scopes, which needs no `globalDir` because the folder does not move with a variable, and the `AGENTS.md` block covers the pointer because Cline reads `AGENTS.md` and `~/.agents/AGENTS.md` as a cross-tool rule source. It takes `readsAlso: ['.claude/skills']`, because Cline loads that folder beside its own, so a project installed for Claude Code and Cline holds the same names twice and the duplicate warning has something to fire on, the collision OpenCode already had. One behaviour is documented by Cline itself and is now recorded in the README and the guide rather than left in the vendor's pages: when a global skill and a project skill share a name, the global one wins. The other agents here do not say which scope wins, so a stale global install can silently outrank a fresh project one. Plugin: Cline accepts a repo-installable plugin, so the door has two halves, but its manifest is not a JSON file like the Kimi and Cursor doors. `cline plugin install <git url>` reads a `cline` field out of the repository's root `package.json`, which makes this the first door that needed a root manifest and the first that changed the repository's own shape to open. The entry it points at, `.cline-plugin/plugin.js`, registers no tools and no hooks: the payload is the `skills/` folder the plugin bundles, which Cline discovers on install, so nothing is copied into a project. Cline's own documentation limits plugins to the SDK, the CLI, and Kanban and says the feature does not apply to the VS Code and JetBrains extensions, and the README and the guide repeat that instead of softening it, so this door covers a CLI install while the installer stays the route for an editor. The root `package.json` joins the version check, so a bump has to reach it too. Both halves of the door, and the CLI itself, still have no live check, and that moves to the next maintenance round. |
| v3.2.15 | Amp, and this time the premise held. The What's next row promised one `.agents/skills` reader confirmed on its own, which is exactly the claim Cline's documentation had broken one version earlier, so it was read the same way before any code was written: Amp documents `.agents/skills/` in the project and in searched parent directories, plus `.claude/skills/` beside them, so it does read the folder Antigravity, Copilot, and Kimi Code already share. It is the fourth member of that group rather than a folder of its own. Two fields part it from Cline's shape. It needs a `globalDir`, because the user-level path it documents is `~/.config/agents/skills/` and not the shared folder, and it takes `readsAlso: ['.claude/skills']`, because it loads that folder in the project and in parent directories, so a project installed for Claude Code and Amp holds the same names twice and the duplicate warning fires. The `AGENTS.md` block covers the pointer, because Amp always includes `AGENTS.md` from the working directory up to the home directory. Eleven agents still resolve to eight project folders. Plugin: Amp gets no door, and this is the first slot where a door was deliberately not opened rather than deferred. Amp does not scan a bundled `skills/` folder, so each skill has to be registered by a call when the plugin loads; single-file plugins cannot register skills at all, and the documented one-command install takes single files only; a directory plugin, which is the only kind that can register skills, has no one-command install from a repository URL; and a skill arriving through a plugin is renamed `<plugin>:<skill>`, which does not match the bare names the pointer block and the core already use. Against Cline's root manifest and two-line entry, that is real cost with no matching benefit, so Amp takes Copilot's shape: the installer is the whole door. The release also corrects two sentences it shipped one version earlier. v3.2.14 said Cline was the only agent here that lets a global skill outrank a project one, which was inferred from the silence of the other vendors rather than from their documentation, and Amp documents the same order explicitly, so the README and the guide now name both and stop speaking for the rest. The guide also counted the agents that read more than one folder as two; it is five, and the others are Codex, Hermes, and OpenCode. The rewrite of that paragraph then named Claude Code as the collision partner for all five, which is true of three of them: OpenCode, Cline, and Amp load `.claude/skills/`, while Codex and Hermes collide with the agents that write `.agents/skills/` instead. A matrix over every agent pair caught it against the code before the release, and the sentence now states both groups. |
| v3.2.16 | The update answer, which is the one item in this slot that came from answering users rather than from reading code, and the reason it needed one. Every route was already documented in the README and the guide, and none of it helped, because a table is organised by route and the reader has to recognise which one they used before any row applies to them. Nothing on disk records it: an installer folder and a skills-directory folder hold the same files, so the two cannot be told apart even by looking. The fix moved the answer to where the question is asked. The pointer block the installer writes into `AGENTS.md` and `CLAUDE.md` now carries the update line, and the core's own update block names the six plugin commands instead of deferring every door to `GUIDE.md`, which is the sentence that made an agent answer "read the guide". The installer gained `--update` as well: it replaces every antislop folder it finds at project and global scope, keeps the skill selection each folder was installed with, prints the release it replaced beside the one it wrote, and asks nothing, so it runs unattended. It deliberately does not add a skill the user never installed, because an update that quietly widens the install is not an update. The scope list is a parameter rather than a constant so the smoke test cannot write into the home directory it runs under, which is the one place a test of this could do real damage. The second half exists because without it `--update` is a half answer. A plugin door keeps its own copy under the vendor's own store, so `--update` cannot reach it, and a user who installed that way would have run the command, been told nothing was found, and had no way to know why. The installer now probes four of those stores and prints the exact update command for each door it finds: Claude Code's `installed_plugins.json`, which carries the version itself; Codex's `plugins/cache/antislop/<version>/`, where the version is the directory name; and the git clones Antigravity and Cursor keep under `.gemini/config/plugins/` and `.cursor/plugins/local/`. It reports a version only where the store supplies one, says unknown rather than guessing when a clone predates the `VERSION` file, and a probe that misses because a vendor moved its layout goes silent instead of failing the run, which is the whole reason the read is wrapped. Every path in that list is internal to a vendor and none is documented as a stable interface, so the code says so where the probes are written. Two doors are deliberately absent. Cline's and Kimi Code's stores are named in their documentation but their layouts are not: Cline's docs stop at `_installed/` and never describe the naming below it, and Kimi's name `installed.json` without naming its parent directory or its shape. Neither tool is installed on this machine, so neither could be checked, and a probe written on a guess is exactly what this slot exists to remove; the four that shipped were each verified against a real install here, which is also where the problem showed itself. Four stale plugin copies sat on the maintainer's own machine, at 3.2.8, 3.2.12, and two clones older than the `VERSION` file, against a current release of 3.2.15, and nothing in the installer had ever mentioned them. The saved usage-mode preference stays open with its author rather than landing here. |
| v3.2.17 | Pi, and the round exists because the pull request that proposed it could not be used. The branch was opened against v3.2.9 and sat while six releases landed under it, so it conflicted on `GUIDE.md`, `README.md`, `ROADMAP.md`, `cli/scripts/smoke-worker.mjs`, and `package.json`; the rebase it needed was asked for and never came, and the pull request was closed with the work taken forward here. Reading the vendor's documentation first is what made that the right call rather than a rewrite. Pi documents `.pi/skills/` for a project and `<agent-dir>/skills/` at user level, which resolves to `~/.pi/agent/skills/`, so the row takes both, and it takes `AGENTS.md` for the pointer. It also walks the shared `.agents/skills/` directory at both scopes, in the project and in searched ancestors up to the repository root, and that is the field the submitted row was missing. Without `readsAlso: ['.agents/skills']`, choosing Pi beside Antigravity, Copilot, Kimi Code, or Amp would have put the same skill names in two folders Pi reads while the duplicate warning stayed silent, which is the defect v3.2.11 fixed for Codex and the reason the field exists. With it, six rows read more than one project folder and twelve agents resolve to nine. Pi is the fifth agent to keep a folder of its own and still read one the installer writes, which puts it in Cline's and Amp's group rather than the shared-folder group Copilot and Kimi Code are in. The trust gate is the second thing the documentation settled. Pi gates everything it loads out of a project, skills included, so the installer has to name the step, but it is not Hermes's step: there is no command to run. Pi asks on the first run, `/trust` saves the answer, `--approve` answers once for an automated run, and `defaultProjectTrust` decides what a non-interactive run does when it cannot ask. The installer says that instead of telling the user to run a command that does not exist, and the guide records the part that is easy to miss: context files are not gated, so `AGENTS.md` loads either way, which means the pointer can sit there saying antislop is installed while the skills stay dark until the question is answered. The package door is the other half. Pi reads a `pi` key in the root `package.json` pointing at `./skills`, and a plain `skills/` directory is read without a manifest at all, so the key is for explicitness rather than necessity; `pi-package` joins the keywords because that is what makes an npm package eligible for Pi's gallery. That key goes into the file v3.2.14 created rather than a new one, because the same root manifest carries the `cline` key the Cline plugin door registers from, and a second file would have unregistered it. Pi was installed afterwards, so this door does not rest on documentation alone. Version 0.87.1 was installed and probed without a model, by driving the resource loader directly, since no provider is configured on this machine. Four things it settled: a project `.pi/skills/` loads, a project `.agents/skills/` loads beside it, both `~/.pi/agent/skills/` and `~/.agents/skills/` load at user scope, and the package door works end to end, with all six skills loading from the `pi` key and no diagnostic reported. It also settled something this row would otherwise have got wrong. Pi resolves a duplicate skill name itself and reports the copy it kept and the copy it dropped, so the collision prose could no longer say that none of these agents settles it: project `.pi/skills/` outranks both the shared `.agents/skills/` and the user-level folder, which the loader was read for and then tested. That is the ordinary direction rather than Cline's and Amp's, so Pi is in neither group and the prose now says both. Two commands the probe corrected on the way: the README's update row said to re-run `pi install`, and `pi update` with no target updates Pi itself rather than its packages, so `pi update --extensions` is the one that reconciles a package. No plugin-door detection was added for Pi against the `packages` array in `~/.pi/agent/settings.json`, which the probe did confirm the shape of. That is a new installer feature rather than a door fix, and it is not this round. Two staleness repairs ride along, both found while checking this door rather than guessed at. The distribution line in this file still said seven install paths and listed five plugin doors, which v3.2.14 had already made wrong when Cline shipped, and it now says nine and names Cline and Pi. The guide's route table said four routes and now says five, which moves the terminal count with it. |
## What's next

Two rules keep the queue honest:

> **One new agent per version.** A version opens at most one new agent's door, so a door that breaks is only ever one agent's problem. Repairs ride along: a break found in a door already shipped is fixed when it is found, never queued behind a slot.

> **A maintenance round ships no new agent.** It pays down what an earlier door left behind, chosen at the time from re-validating each door against its vendor's current documentation and from what the community reports. The slot is fixed; its contents are not.

Every door is verified against the vendor's current documentation before its version ships rather than assumed, and the installer and the guide are updated in the same release.

| Version | Item | What it means |
|---------|------|---------------|
| v3.2.18 | Oh My Pi | Oh My Pi is a fork of Pi and falls back to the `pi` key v3.2.17 added to the root `package.json`, so the first question is what the plugin manifest adds that the key does not, and the answer decides whether `.omp-plugin/` ships at all. A community PR opened the folder on main ahead of this slot, so the slot is now about keeping it rather than adding it, and the same question decides that. Its catalog entry carries a version either way, because Oh My Pi's update checker permanently skips entries that lack one. Oh My Pi is not installed on this machine, so no probe exists for any of it: the commands in the README and the guide come from the contributor, and the door is checked against a real install before this version ships rather than assumed from the manifest. The open question about the contrast MCP server it declares, which the Codex, Cursor, and Kimi doors left out, is settled in the same round. |
| v3.2.19 | Maintenance | No new agent, and the slot carries what v3.2.16 did not: both halves of the Cline door still have no live check, and the saved usage-mode preference lands here if its author brings the Windows settings path and the fresh-session proof. Two further items are candidates the same way every maintenance slot picks its own. The plugin-door probes are the first surface here that reads a vendor's private store rather than a documented one, so they are re-checked against a real install before the round ships, and a door whose layout moved is corrected or dropped rather than left to go quiet. Cline's and Kimi Code's stores stay out until their layouts can be read from documentation or from an install, which is what kept them out of v3.2.16. Pi's package belongs in the same pass, now that v3.2.17 confirmed the `packages` array in `~/.pi/agent/settings.json` against a real install: a user who installed the package is told nothing today, and the installer could name `pi update --extensions` the way it already names a plugin door's command. |

Beyond the numbered plan, with no promised version:

- **OpenAI public directory listing** (deferred). Listing prep for the plugin directory shared by ChatGPT and Codex: a versioned skill bundle, a submission document, and public privacy and terms pages. The submission happens on the OpenAI Platform after identity verification, outside this repo; parked until that can proceed.
- **Gemini CLI** (deferred). Consumer access ended on 18 June 2026 and Antigravity CLI replaced it, which already has a door (v3.2.2). It was not a total shutdown: enterprise Code Assist licenses and paid API keys still work, and the Apache 2.0 repository is still maintained, so `.gemini/skills/` stays. A completeness close would need an extension manifest for a tool whose consumer audience is gone; parked with antislop-compact.
- **Kiro** (candidate). Kiro reads agent skills from `.kiro/skills/` per project and `~/.kiro/skills/` globally, activating them by description. It sits outside the shared `.agents` standard, so it would be an installer row and an `AGENTS.md` pointer rather than a marketplace door.
- **Roo** (candidate). Sources disagree on its skill folder: some list `.roo/skills/`, others route it to `.roo/rules/`, and its own setup guides describe `.roo/rules/*.md` discovery rather than skill directories. It is a candidate rather than a long-tail row because the path has to be settled against Roo's own documentation before it can be a version.
- **More plugin doors** as other agents grow plugin systems that antislop can ride from the same repo.
- **antislop-compact**, a lightweight, standalone family of the five skills, each a self-contained cheat-sheet version that runs without the core alongside. Deferred: a parallel set of files would have to track every change to the full ones, and a compact skill is only sound if it keeps the one-line why per rule and the Delivery Gate.
- **Skill candidates** still open: `antislop-docs` and `antislop-identity`.

## Not in scope

antislop stays a **filter, not a style guide**:

- No prescribed aesthetics, per-framework recipes, or trend bans.
- It never beautifies on its own; direction and beauty are yours, in `DESIGN.md`.
- It is not limited to building pages: the same filter writes and audits copy (`antislop-copywriting`).
- UX and motion are folded into `antislop-ui` rather than separate skills, and data-integrity rules already live in the core.
